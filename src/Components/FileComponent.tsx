import {StyleSheet, View} from 'react-native';

function FileComponent() {
  return <View style={styles.white}>hello</View>;
}

const styles = StyleSheet.create({
  white: {
    backgroundColor: '#61dafb',
  },
});

export default FileComponent;
